#include "main.h"
#include <stdio.h>
/**
 * _strcat - concanates two strings
 * @dest: the destination
 * @src: the source file
 * Return: returns dest
 */

char *_strcat(char *dest, char *src)
{
	printf("%s%s\n", dest, src);
}
