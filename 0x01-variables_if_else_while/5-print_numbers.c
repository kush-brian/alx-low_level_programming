#include <stdio.h>

/**
 * main - the entry point
 *
 * Description: print numbers
 *
 * Return: return 0 when successful
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}

	return (0);
}
